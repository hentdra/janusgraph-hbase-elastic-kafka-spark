package com.ebdesk;

import org.apache.commons.configuration.BaseConfiguration;
import org.apache.tinkerpop.gremlin.process.traversal.dsl.graph.GraphTraversalSource;
import org.apache.tinkerpop.gremlin.structure.Graph;
import org.apache.tinkerpop.gremlin.structure.util.GraphFactory;
import org.janusgraph.core.JanusGraph;
import org.janusgraph.core.JanusGraphFactory;
import org.janusgraph.core.JanusGraphTransaction;
import org.janusgraph.core.util.JanusGraphCleanup;

public class OlapExample {

    public static void main(String args[]){


//        GraphTraversalSource gr = graph().traversal();
//        JanusGraphTransaction tx = tx = graph().tx().createThreadedTx();
//        System.out.println(tx.traversal().V().next());
//        BaseConfiguration conf = confHbase();
//        JanusGraph graph = JanusGraphFactory.open(conf);
//        GraphTraversalSource g = graph().traversal();
//
//        graph.close();
//        JanusGraphCleanup.clear(graph);
//        graph = JanusGraphFactory.open(conf);

    }
}
